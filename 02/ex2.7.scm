#lang scheme
(define (add-interval x y)
  (make-interval
   (+ (low x) (low y))
   (+ (high x) (high y))
   )
  )
(define (sub-interval x y)
  )
(define (mul-interval x y)
  (let (()
        ())
    ()
    )
(define (div-interval x y)

  )
(define (make-interval a b)
  (cons a b)
  )
(define (low interval)
  
  )
(define (high interval)

  )